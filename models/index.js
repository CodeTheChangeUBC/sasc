const user = require('./users');
const counsellor = require('./counsellors');
const message = require('./messages');
const session = require('./sessions');

module.exports = {
	user,
	counsellor,
	message, 
	session,
}